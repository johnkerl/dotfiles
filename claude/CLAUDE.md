# Global preferences

- When the user runs a shell command themselves (via the `!` prefix, shown as `<bash-input>`/`<bash-stdout>` in the transcript), do not comment on it or take a turn. It's for their own use — just stay silent unless they address you directly.
